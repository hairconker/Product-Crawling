# 阶段 7 · 文档、测试与上线

> 周期：第 16 天
> 目标：交付可维护、可二次开发的项目。

---

## 任务清单

### 测试
- [ ] 7.1 `tests/unit/`：纯单元测试（不联网）
  - logger / exceptions / models / settings
  - 各 spider 的 `_parse_*` 函数（用 fixtures 里的 HTML/JSON 样本）
- [ ] 7.2 `tests/integration/`：标记 `@pytest.mark.integration`，需要真实网络与 cookie
  - 每个平台 1 个 happy path
  - CI 默认跳过，本地 `pytest -m integration` 运行
- [ ] 7.3 `tests/fixtures/`：保存各平台真实样本响应（脱敏后）
- [ ] 7.4 GitHub Actions：每天定时跑一次 smoke test，失败发 issue

### 文档
- [ ] 7.5 `README.md`：项目简介、安装、快速上手、各平台 cookie 获取教程、调用示例
- [ ] 7.6 `docs/troubleshooting.md`：踩坑清单（按平台分章节）
- [ ] 7.7 `docs/extending.md`：如何新增一个平台 spider
- [ ] 7.8 `CHANGELOG.md`：版本记录

### 上线
- [ ] 7.9 `Dockerfile` 多阶段构建，最终镜像 < 1GB
- [ ] 7.10 `docker-compose.yml`：API + xvfb（用于 Playwright headed）+ SQLite volume
- [ ] 7.11 编写 `scripts/deploy.sh`：一键部署到自有服务器

---

## 验收标准

- `pytest tests/unit/` 全绿，覆盖率 > 70%
- 新人 clone 仓库后照 README 操作 30 分钟内能跑通至少 1 个平台
- `docker compose up -d` 后 API 可用
- `docs/troubleshooting.md` 至少覆盖 10 个常见问题
